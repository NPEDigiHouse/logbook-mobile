import 'package:flutter/material.dart';

class ListCasesPage extends StatelessWidget {
  const ListCasesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}