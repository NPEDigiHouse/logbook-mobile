import 'package:flutter/material.dart';

class ListSkillsPage extends StatelessWidget {
  const ListSkillsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
